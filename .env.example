# Highway Core Environment Variables

# Database URL for SQLAlchemy
# For SQLite, use: sqlite:///path/to/your/database.db
# For PostgreSQL, use: postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///highway.sqlite3

# --- PostgreSQL Settings (Optional) ---
# If all 5 are set, Highway will use PostgreSQL.
# Otherwise, it will fall back to the SQLite DATABASE_URL above.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
