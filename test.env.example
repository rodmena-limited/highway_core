# Highway Core Test Environment Variables

# Database URL for tests (in-memory SQLite is a good option)
DATABASE_URL=sqlite:///:memory:

# Redis connection details for tests
# For local testing with a real Redis instance, you can use the following:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=1 # Use a different DB for tests

# To use fakeredis for tests (recommended), set the following variable:
USE_FAKE_REDIS=true
