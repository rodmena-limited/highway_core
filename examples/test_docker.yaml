name: "Test Docker Runtime"
start_task: "run_docker"

variables: {}

tasks:
  run_docker:
    task_id: "run_docker"
    operator_type: "task"
    runtime: "docker"
    image: "alpine:latest"
    command:
      - "echo"
      - "Hello from Docker"
    result_key: "docker_output"